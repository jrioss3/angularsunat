import { AddParentesisPipe } from './add-parentesis.pipe';

describe('AddParentesisPipe', () => {
  it('create an instance', () => {
    const pipe = new AddParentesisPipe();
    expect(pipe).toBeTruthy();
  });
});
