import { AgregarSignoPeriodoPipe } from './agregar-signo-periodo.pipe';

describe('AgregarSignoPeriodoPipe', () => {
  it('create an instance', () => {
    const pipe = new AgregarSignoPeriodoPipe();
    expect(pipe).toBeTruthy();
  });
});
