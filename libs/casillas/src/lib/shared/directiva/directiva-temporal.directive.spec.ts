import { DirectivaTemporalDirective } from './directiva-temporal.directive';

describe('DirectivaTemporalDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaTemporalDirective();
    expect(directive).toBeTruthy();
  });
});
